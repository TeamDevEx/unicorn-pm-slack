name: Hello POGS

on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Print Hello POGS!
      run: |
        echo "HELLO POGS!"
